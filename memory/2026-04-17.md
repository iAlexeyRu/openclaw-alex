# 2026-04-17

- Первый разговор в Telegram.
- Определена личность ассистента: Алиса, помощница, ироничная, эмодзи 😤.
- Пользователь: Алексей Гетманец, обращаться «Алекс», часовой пояс Europe/Moscow.
- Предпочтения по стилю: аккуратность важнее скорости, ирония может быть ядовитой, тупняк не приветствуется 😄.
- Сгенерирован отдельный SSH-ключ для GitHub-доступа: `~/.ssh/id_ed25519_openclaw_github`.
- Подтянула старый workspace-репозиторий `openclaw-alex` и перенесла оттуда долгую память, heartbeat-настройки, README, git-policy и локальные skill'ы.
- Настроила `origin` на `git@github.com:iAlexeyRu/openclaw-alex.git`, но write-доступ по SSH ещё не подтверждён этим хостом.
- Проверила skill `1c-urok`: логин на `urok.1c.ru` с сохранёнными workspace-учётными данными проходит успешно, доступны страницы `/materials/`, `/personal/profile/` и `/library/`.
- Для полноценного Playwright-браузера на этом хосте пока не хватает системных библиотек (`libnspr4` и, вероятно, соседних зависимостей), но HTTP/AJAX-доступ к порталу уже работает.
- Проверила Lightpanda как лёгкую headless-альтернативу: nightly-бинарник на этом хосте запускается, `fetch` по `urok.1c.ru/library/` частично работает, но связка Lightpanda + Playwright/CDP на этом сценарии пока нестабильна и закрывает страницу при переходе.
- Установила локальный Lightpanda tool в workspace через `scripts/install_lightpanda.sh`, добавила benchmark-скрипты для `1c-urok` и прогнала сравнение `requests` vs `lightpanda` на защищённой странице профиля.
- По замеру 2026-04-17: `requests` в среднем около 0.575 с и 32.6 MB RSS, `lightpanda` около 1.141 с и 103.9 MB RSS на этом конкретном сценарии.
- Нашла отдельный сценарий, где Lightpanda реально полезнее: React shopping cart (`react-shopping-cart-67954.firebaseapp.com`). Там `requests` быстрее и легче, но видит только HTML-оболочку, а Lightpanda вытаскивает весь JS-рендеренный список товаров (4/4 маркеров против 0/4 у `requests`).
- Добавила общий URL benchmark tool (`scripts/benchmark_urls.sh`, `scripts/run_url_set_benchmarks.py`) и прогнала его на Meduza, GitHub, Championat и Steam. По чистой скорости `requests` выиграл на всех 4, но на Championat он получает только `403`, тогда как Lightpanda хотя бы вытягивает страницу.
